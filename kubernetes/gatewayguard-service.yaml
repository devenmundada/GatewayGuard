apiVersion: v1
kind: Service
metadata:
  name: gatewayguard-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: gatewayguard
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30080
